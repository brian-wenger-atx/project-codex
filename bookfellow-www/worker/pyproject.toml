name = "bookfellow-worker"
version = "0.1.0"
description = "Bookfellow BullMQ Python worker (lab)"
requires-python = ">=3.12"
dependencies = [
  "bullmq>=2.9.0",
  "pydantic>=2.11.0",
  "redis>=5.2.0",
  "psycopg[binary]>=3.2.0",
]

[project.scripts]
bookfellow-worker = "worker.main:main"
