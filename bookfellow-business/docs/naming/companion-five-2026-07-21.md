# Companion five — Bookside / Storymate / Bookfellow / Pageside / Alongside (2026-07-21)

| Name | Pitch line | Domain | Existing use | Take |
|------|------------|--------|--------------|------|
| **Pageside** | Always on the page's side. | `.com`/`.app` taken; **`.io` / `.cc` likely free** | Irish holding co. at pageside.com — not books | **Best runway** |
| **Bookfellow** | Your bookfellow between sittings. | `.com` taken; **`.io` / `.cc` likely free** | BookFellows = bible/lit study platform | Strong word; mild plural clash |
| **Bookside** | Bookside — where the companion sits. | `.com`/`.io`/`.app`/`.net` taken; **`.cc` / getbookside.com likely free** | **BookSide** App Store — family reading/library | Nice word; category collision |
| **Alongside** | Reads with you — never instead of you. | `.com`/`.io`/`.cc`/`.app` taken (HR ATS owns `.com`) | Alongside.com = hiring software | Best thesis; hardest to own |
| **Storymate** | A mate for the story in progress. | majors taken; `.cc` maybe free | Multiple **StoryMate** AI story-generator apps | **Drop** — AI + story-creation clash |

Confirm frees at a registrar before buying.
